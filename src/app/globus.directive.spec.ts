import { GlobusDirective } from './globus.directive';

describe('GlobusDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobusDirective();
    expect(directive).toBeTruthy();
  });
});
